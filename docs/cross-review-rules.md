# 规则解释器交叉复审与主 agent 收尾

2026-09-20。指定 gpt-6-astra 追加复审最终返回模型不支持错误，不能声称该子 agent 完成最终审批。它已写入的 test_cross_review_rules.py 保留并执行。

主 agent 复查允许的 AST/数据方法、有限内建、禁止任意 callable/宿主属性/dunder、复制输入与循环/表达式/集合/字符预算；现有五个固定内置规则 735 个场景逐条对照，结果及原因保持一致。

初次交叉 17 项中两个持久化用例失败：保存配置失败后规则文件已经覆盖或新增，产生“失败但生效”。现改为配置 source manifest 是唯一发布提交点，源码、enabled、order 同时原子替换；data/interceptors 中的文件仅作兼容镜像。镜像失败仍从已提交 manifest 读取，旧代码不被未提交文件替换。没有删除失败断言。

新增：配置失败不覆盖已有自定义规则、镜像文件不可写时已提交规则仍可读取执行。最终该组 19/19，通过隔离 runner，无真实插件上传、服务器或交易操作。

边界：此实现不是任意 Python 沙箱。新规则必须在受限纯数据子集内；部署前验证已启用规则可解析。文件权限/OS资源隔离和外部服务不能以离线测试全部保证。超管上传仍属于受信任策略配置变更，但不再授予任意宿主Python执行能力。
