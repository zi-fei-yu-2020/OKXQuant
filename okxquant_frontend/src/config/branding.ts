import packageInfo from '../../package.json'

/** Shared branding; package.json is the release-version source of truth. */
export const APP_VERSION = `v${packageInfo.version}`
export const APP_LOGO_SRC = '/favicon.svg?v=okxquant-20260915'
